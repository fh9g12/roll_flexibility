function set_constants(p,U)
% fold = p.get_fold(U);
% p.theta_t = p.get_theta_t(U);
% p.k_h = fold_stiffness(fold,p.wall_transition,p.wall_k,p.limits);
% p.d_h = fold_stiffness(fold,p.wall_transition,p.wall_d,p.limits + deg2rad([0.5,-0.5]*p.wall_transition));
p.k_h = 0;
p.d_h = 0;
end


